# Segredos / credenciais — NUNCA commitar
.env
.env.*
!.env.example

# Stack legado aposentado (Story 009) — não envia para produção
legacy/

# Paginas de diagnostico: publicaveis (vao pro deploy via build.js), MAS
# o painel index.html (agrega todos os leads) e os _copy.txt ficam fora
huberick-temp/index.html
huberick-temp/*_copy.txt
public/huberick-temp/

# Node
node_modules/
package-lock.json
.next/
tsconfig.tsbuildinfo

# Cache local do enriquecimento de leads (Story 017) — HTML coletado, não commitar
.cache/

# Scaffold Next.js temporário (lixo de migração)
tmp-next-app/

# OS
Thumbs.db
.DS_Store
lista-de-ataque.html

# Dados do disparo de email (PII) — nao commitar
scripts/email/email_queue.json
scripts/email/sent_log.json
# Segundo e-mail da sequencia (18/09/2026): fila por dia e log proprio, mesma PII.
scripts/email/email_queue_seq2_*.json
scripts/email/sent_log_seq2.json
# Enderecos bloqueados por hard bounce/spam: e lista de e-mail de lead real.
scripts/email/blocklist.json
# Colheita de e-mails (18/09/2026): cada arquivo lista e-mail, decisor e CNPJ de lead.
scripts/email/colheita/
preview-emails.html

# Reconciliacao WhatsApp x CRM (PII: telefones e transcricao de conversa)
reconciliacao-whatsapp.md

# Relatorio gerado por scripts/analise-conversas.mjs --md (cita conversa de lead)
data/analise-conversas.md

.vercel
.env.local
logs/

# Backups de dados de leads gerados por scripts: contem dado pessoal, nao vao pro repo.
scratchpad/backup-*.json
data/analise-respostas.md
